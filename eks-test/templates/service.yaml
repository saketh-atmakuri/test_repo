apiVersion: v1
kind: Service
metadata:
  name: {{ include "eks-test.fullname" . }}
  labels:
    {{- include "eks-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-test.selectorLabels" . | nindent 4 }}
